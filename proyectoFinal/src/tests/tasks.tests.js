import MongoDao from '../persistence/daos/mongodb/mongoDao.js';
import chai from 'chai'
const { expect, assert } = chai;
import mongoose from 'mongoose';

const doc = {
    title: '',
    body: '',
    author
}

