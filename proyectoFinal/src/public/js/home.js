/*

const productsJSON = products;

/*async function getProductFromFile() {
    productsJSON = await productManager.getProducts();
}


function renderizarProductos() {
    //getProductFromFile();
    console.log(productsJSON);
    for (const prod of productsJSON) {
        document.getElementById('productsList').append(
            `<li><div class="card" style="width: 12em;" style="height:12em;">
            <img src="${prod.thumbnails}" class="card-img-top" alt="${prod.title}">
            <div class="card-body">
            <h5 class="card-title">${prod.title}</h5>
            <p class="card-text">$${prod.description}</p>
            <p class="card-text">$${prod.code}</p>
            <p class="card-text">$${prod.price}</p>
            <p class="card-text">$${prod.stock}</p>
            <p class="card-text">$${prod.category}</p>
            </div>
            </div>
            </li>`
        )
    }
}

renderizarProductos();
*/